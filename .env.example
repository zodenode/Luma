OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Legacy header auth for webhooks (x-webhook-secret)
OPENLOOP_WEBHOOK_SECRET=dev-openloop-secret
PHARMACY_WEBHOOK_SECRET=dev-pharmacy-secret
# HMAC body signing (x-signature: sha256=<hex>) — preferred in production
OPENLOOP_WEBHOOK_HMAC_SECRET=
PHARMACY_WEBHOOK_HMAC_SECRET=
# Optional: override JSON store path (default data/luma.json)
# LUMA_DB_PATH=data/luma.json
